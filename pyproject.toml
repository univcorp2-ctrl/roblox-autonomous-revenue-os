[project]
name = "roblox-autonomous-revenue-os"
version = "0.1.0"
description = "Control plane for autonomous Roblox revenue experiments"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
revenueos = "revenueos.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
